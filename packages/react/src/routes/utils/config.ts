const AUTH_PATH = '/auth';

const SIGN_UP_PATH = `${AUTH_PATH}/sign-up`;
const LOGIN_PATH = `${AUTH_PATH}/login`;

const INDEX_PATH = '/';

export default {
  LOGIN_PATH,
  AUTH_PATH,
  SIGN_UP_PATH,
  INDEX_PATH,
};
