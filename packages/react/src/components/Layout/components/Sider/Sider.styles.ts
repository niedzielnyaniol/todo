import { styled } from '@compiled/css-in-js';

const StyledLogo = styled.div`
  display: flex;
  align-items: center;
  padding-left: 10px;
  height: 32px;
  background: rgba(255, 255, 255, 0.2);
  margin: 14px;
`;

export { StyledLogo };
